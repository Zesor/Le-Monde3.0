version: '3.7'

services:
  postgres:
    image: postgres
    environment:
      - POSTGRES_USER=hackathon
      - POSTGRES_PASSWORD=thisispassword
      - POSTGRES_DB=postgres
    ports:
      - 5432:5432
    volumes:
      - ./data_container:/backend/database/data/

  # db:
  #   build: ./backend/database/
  #   ports:
  #     - "9020:9020"
  #   networks:
  #     - db-tier
